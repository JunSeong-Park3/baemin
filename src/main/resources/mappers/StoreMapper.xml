<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.han.delivery.dao.StoreMapper">

	<select id="storeList"
		resultType="com.han.delivery.dto.StoreDto">
		select * from dl_store where category = #{category} and storeAddress1 >= #{address} || '%'
	</select>
	
	<select id="storeDetail" resultType="com.han.delivery.dto.StoreDto">
		select * from dl_store where id = #{storeId}
	</select>
	
	<select id="foodList" resultType="com.han.delivery.dto.FoodDto">
		select * from dl_food where storeId = #{storeId}
	</select>
	
	<select id="foodOption" resultType="com.han.delivery.dto.FoodOptionDto">
		select * from dl_food_option where foodId = #{foodId}
	</select>
	
</mapper>